[package]
name = "mnz-chain-rust"
version = "1.0.0"
edition = "2021"

[dependencies]
actix-web = "4.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = "0.4"

# Lock to compatible versions (no edition2024)
time = "=0.3.36"
getrandom = "=0.2.15"
indexmap = "=2.13.0"
